import React, {useEffect, useState} from 'raect';
import Axios from 'axios';

import React from 'react'

function Getwhether() {
    const [data, setdata] = useState([]);
    useEffect(() => {
        
    }, [])
}

export default Getwhether;
